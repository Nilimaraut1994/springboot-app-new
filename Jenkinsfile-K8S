@Library('My-Jenkins-SharedLibrary@feature/1.0') _
pipeline {
    agent any
       tools {
        maven 'Maven3'
    }
    stages {
        stage ('Build Project') {
          steps {
              script{  
                  java_build()
                }  
            }
        }     
    }
}
