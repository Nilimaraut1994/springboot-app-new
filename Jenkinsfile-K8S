@Library('My-Jenkins-SharedLibrary') _
pipeline {
    agent any
       tools {
        maven 'Maven3'
    }
    stages {
        stage ('Build Project') {
          steps {
              script{  
                  java_build()
                }  
            }
        }     
    }
}
